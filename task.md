# task.md
- [x] Abort Supabase Edge Functions migration.
- [x] Create `render.yaml` configuration for Render deploy.
- [ ] Push code to GitHub (`git add .`, `git commit`, `git push`).
- [ ] Connect the GitHub repository to Render (https://dashboard.render.com/select-repo).
- [ ] In Render settings, add the Environment Variables from `.env`.
- [ ] Update Africa's Talking USSD and WhatsApp callbacks to your new Render URL (e.g. `https://magri-backend.onrender.com/ussd`).
